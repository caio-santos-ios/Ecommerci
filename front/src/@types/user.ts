export type TuserLogin = {
    email: string;
    password: string;
} 

export type TuserRegister = {
    name: string;
    email: string;
    password: string;
}

export type TuserToken = {
    token: boolean;  
}