import { styled } from "styled-components";

export const StyleListStock = styled.ul`
    width: 50vw;

    margin: 0 auto;
    padding: 0;

    div {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 0;
        margin: 0;

        > h2 {
            padding: 0;
            margin: 0;
        }
    }
`